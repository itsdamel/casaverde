import styled from 'styled-components'
import BaseFlexDiv from '../Base/BaseFlexDiv';

const Card = styled(BaseFlexDiv)`
    width: 60vw;
    height: 50vh;
    justify-content: center;
    box-shadow: 10px 10px 30px rgba(0, 0, 0, 0.06);
    z-index: 1;

`
export default Card;